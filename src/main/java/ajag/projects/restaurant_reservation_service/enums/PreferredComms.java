package ajag.projects.restaurant_reservation_service.enums;

/** Enum representing the preferred communication methods for a customer. */
public enum PreferredComms {
    EMAIL, // notifications and reminders will be sent to the email address provided
    SMS // notifications and reminders will be sent to the phone number provided
}